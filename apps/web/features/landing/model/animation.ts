import type { Transition } from "framer-motion";

export const fadeUpTransition = {
  duration: 0.67,
  ease: [0.22, 1, 0.36, 1],
} satisfies Transition;

export const HERO_TEXT_DELAY_SECONDS = 3;
export const HERO_HEADING_DELAY_SECONDS = 3.6;
export const HERO_BUTTON_DELAY_SECONDS = 4.2;
